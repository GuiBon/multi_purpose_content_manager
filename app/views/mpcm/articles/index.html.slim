h1 Articles

= link_to 'Nouvel article', new_article_path, class: 'btn btn-primary space-2'

table.table.space-4
  thead
    tr
      th Titre
      th Date de création
      th Publié ?
      th colspan='3'
  tbody
    - @articles.each do |article|
      tr
        td = article.title
        td = article.formatted_creation_time
        td = article.formatted_published
        td = link_to 'Voir', article_path(article)
        td = link_to 'Modifier', edit_article_path(article)
        td = link_to 'Supprimer', article_path(article), method: :delete, data: { confirm: 'Êtes-vous sûr ?' }

= link_to 'Nouvel article', new_article_path, class: 'btn btn-primary space-2'
